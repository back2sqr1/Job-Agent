{
  "//": "OPT-IN: copy to config/credentials.json (gitignored) to let the apply flow sign in to ATS accounts for you. The password sits in plaintext on your disk — consider `chmod 600 config/credentials.json`. Omit `email` to reuse the email from profile.json. Delete the file to turn the feature off.",
  "//2": "createAccounts is a SECOND opt-in (default false): when true, account-creation forms are completed fully automatically — including ticking the 'I agree to the terms' checkbox and clicking Create Account. Setting it means you are delegating that terms-of-service agreement to the tool. Marketing/job-alert checkboxes are never ticked either way, and email-verification codes remain yours to enter.",
  "//3": "Keys are per ATS: 'workday' and 'ashby' are recognized today (Ashby applications are usually open, so its entry is rarely needed). Include only the ones you want automated.",
  "workday": {
    "email": "jane@example.com",
    "password": "your-workday-password",
    "createAccounts": false
  },
  "ashby": {
    "email": "jane@example.com",
    "password": "your-ashby-password",
    "createAccounts": false
  }
}
