{
  "//": "OPT-IN: copy to config/credentials.json (gitignored) to let the apply flow sign in to ATS accounts for you. The password sits in plaintext on your disk — consider `chmod 600 config/credentials.json`. Omit `email` to reuse the email from profile.json. Delete the file to turn the feature off.",
  "workday": {
    "email": "jane@example.com",
    "password": "your-workday-password"
  }
}
